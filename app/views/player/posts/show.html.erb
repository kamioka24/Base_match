<div class="container">
	<div class="post_show_box">
		<%= link_to @post.team.name, player_team_path(@post.team), class: "post_team_name" %>
		<%= link_to root_path do %> <!-- コンタクトを作成するまでとりあえず -->
			<i class="fas fa-paper-plane"> お問い合わせ</i>
		<% end %>
		<div class="post_title">
			<%= @post.title %>
		</div>
		<div class="post_date">
			<%= @post.created_at.strftime("%Y/%m/%d") %>
		</div>
		<div class="post_img">
			<%= attachment_image_tag @post, :post_image, :fill, 800, 500 %>
		</div>
		<div class="post_body">
			<%= safe_join(@post.body.split("\n"), tag(:br)) %> <!-- 投稿時の改行が反映される -->
		</div>
		<%= link_to "前の画面へ戻る", :back, class: "back_btn" %>
	</div>
</div>